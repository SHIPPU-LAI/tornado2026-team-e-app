# public/ ── 静的ファイル

CSS・クライアントJS・画像素材を置く場所です。

```
public/
├ style.css      共通スタイル（未作成）
└ assets/        画像素材（のれん・雲・和柄・アイコンなど。未用意）
```

## 未決事項

- **Cloudflare の Assets バインディングをまだ設定していません。**
  静的配信を使うなら `wrangler.jsonc` に `assets` の設定が要ります。
  紹介機能の仕様書には `ASSETS`（`./public`）というバインディングの記載がありますが、
  こちらではまだ設定していません
- ディレクトリ構成は仮です。フロントエンド担当の方針に合わせて変えてください
