# 設計書

**2026-09-02 / [要件定義書.md](./要件定義書.md) の前提で書く**

---

## 0. 方針

**仕様書に書かれている API の形は変えない。**
当初は髙橋さんが戻ってきたときに差し替えられるようにするための方針だったが、
2026-09-03、髙橋さん・飯室さんはもう戻ってこない想定になった。
それでも仕様書は実在の資料であり、そこに合わせて設計した判断には根拠があるため、
この方針自体は変えない。仕様書に無いもの（認証・英語）だけを足す。

**他の機能が所有するテーブルに列を足さない。**
英語の文章も `cards` に列を足さず、別テーブルに外出しする。

---

## 1. テーブル

### 1-1. 既にあるもの（変更しない）

```
cards            紹介機能が所有。仕様書どおり。列を足さない
card_embedding   検索機能が所有。稼働中
synonym          検索機能が所有。稼働中
```

### 1-1-b. `cards` とシノニム辞書を migrations に移す（2026-09-03 決定）

当初 `cards` は `dev-schema/cards.sql` に置き、**migrations に入れなかった。**
理由は「`cards` は紹介機能が所有するテーブルで、向こうのマイグレーションが正になるため」。

**この前提が崩れた。**紹介機能はこちらが実装し、担当者は戻ってこなかった。
このままだと**本番で `cards` が作られず、全APIが500になる。**

同じ問題がシノニム辞書にもある。**78語が `seeds/local_seed.sql` の中にしかない。**
本番で seed を流さないと**段2（言い換え検索）が完全に動かない。**
しかしその seed には `delete from cards;` が入っていて本番に流せない。

#### 決定

```
新しいマイグレーションを1つ作る
  ├ create table if not exists cards     dev-schema から移す
  └ insert or ignore into synonym …      78語。段2に必須
                                          synonym は primary key (term, maps_to) なので
                                          or ignore で再実行しても重複しない

seeds/demo_cards.sql を新設          delete を含まない。本番に入れるかは任意
seeds/local_seed.sql                 ローカル専用のまま残す（delete 込み）
dev-schema/                          不要になるので削除する
```

これで `migrations apply --remote` だけで**動く状態**になる。

**`seeds/local_seed.sql` を `--remote` で流してはいけない**という制約は変わらない。

### 1-2. 仕様書にあり、今回作るもの

```sql
-- 記事単位のいいね
create table if not exists likes (
  id         integer primary key autoincrement,
  user_id    text not null,
  card_id    text not null,
  created_at integer not null,
  unique(user_id, card_id)          -- 二重いいねを DB で防ぐ
);
create index if not exists idx_likes_user on likes(user_id);
create index if not exists idx_likes_card on likes(card_id);
```

`swipes` `reviews` `tag_categories` `tags` は**今回作らない**。
理由：swipes は伝統名グループ化をやめるため不要。reviews はスコープ外。
タグは `cards.tags`（カンマ区切り）で足りる。

`card_images` は**作る**（画像アップロードを必須にしたため。仕様書と同じ形）。

### 1-3. 今回あらたに設計するもの

```sql
-- 利用者・職人
create table if not exists users (
  id            text primary key,          -- uuid
  email         text not null unique,
  password_hash text not null,             -- PBKDF2-SHA256
  password_salt text not null,
  role          text not null,             -- 'artisan' | 'user'
  display_name  text,
  created_at    integer not null
);

-- ログインセッション
create table if not exists sessions (
  token      text primary key,             -- 乱数32バイトのhex
  user_id    text not null,
  expires_at integer not null
);
create index if not exists idx_sessions_user on sessions(user_id);

-- 住所の緯度経度（cards に列を足さないための外出し）
create table if not exists card_geo (
  card_id    text primary key,
  lat        real not null,
  lng        real not null,
  source     text,                          -- 'zipcloud' | 'nominatim' | 'map_click'
  updated_at integer not null
);

-- 画像（KV に実体、D1 にキーだけ）
create table if not exists card_images (
  id         integer primary key autoincrement,
  card_id    text not null,
  image_key  text not null,                 -- KV上のキー card-image:{uuid}
  sort_order integer not null,              -- 0..2
  created_at integer not null
);
create index if not exists idx_card_images on card_images(card_id, sort_order);

-- カードの多言語版（cards に列を足さないための外出し）
create table if not exists card_i18n (
  card_id     text not null,
  lang        text not null,               -- 'en' など。'ja' は cards 本体
  name        text,
  description text,
  updated_at  integer not null,
  primary key (card_id, lang)
);
```

**`cards.artisan_id` には `users.id` を入れる。**仕様書の `artisan_id` と同じ位置なので、
髙橋さんの実装と互換が保てる。

**マイグレーション名：** `202609030900_introduce_auth.sql`
（連番だと他の人と衝突するため時刻ベース。AGENTS.md の規約どおり）

---

## 2. API

### 2-1. 仕様書どおりに立てるもの

```
GET  /api/introduce/user/next                   次のカードを1枚
GET  /api/introduce/user/liked                  いいね一覧
GET  /api/introduce/user/:id                    記事詳細
POST /api/introduce/user/:id/like               いいねトグル → { liked, like_count }

GET  /api/introduce/artisan/mine                自分のカード一覧
GET  /api/introduce/artisan/:id                 カード1件（修正フォーム用）
POST /api/introduce/artisan                     新規登録 → { ok, id }
PUT  /api/introduce/artisan/:id                 修正
DELETE /api/introduce/artisan/:id               削除
POST /api/introduce/artisan/name-kana           ふりがな生成 → { kana }
GET  /api/introduce/artisan/name-suggestions    伝統名の予測変換
```

**仕様書との違い（意図的）**

| 仕様書 | 本書 | 理由 |
|---|---|---|
| `GET /next` は伝統名でグループ化 | **グループ化しない** | 在庫が尽きるため（要件定義書 3-2） |
| `user_id` をクエリ・bodyで受け取る | **セッションから取る** | 認証を作るため。なりすませてしまう |
| `POST /:id/swipe` | **作らない** | グループ化しないので不要 |
| `POST /:id/review` | **作らない** | スコープ外 |
| `/traditions/articles` | **作らない** | グループ化しないので不要 |

### 2-2. 今回あらたに作るもの

```
POST /api/auth/signup      { email, password, role, display_name } → Cookie + { user }
POST /api/auth/login       { email, password }                     → Cookie + { user }
POST /api/auth/logout                                              → Cookie削除
GET  /api/auth/me                                                  → { user } / 401

POST /api/introduce/artisan/compose   ★カード作成の中核（次章）
```

### 2-3. パスの衝突確認

`features/search/` が持つのは `/api/search/*` と `/dev/search`。
`features/register/` は削除した（8-3参照）。
**`/api/introduce/*` と `/api/auth/*` は誰とも衝突しない。**

`GET /api/introduce/user/:id` は `:param` なので、後から
`/api/introduce/user/liked` のような静的パスを足すと**先に書いたほうが勝つ**。
**静的パスを先に、`:id` を最後に書く。**

---

## 3. カード作成（本体）

### 3-1. 何が問題か

職人の困りごとの一つが「自分の技術をどう説明するか」そのもの。
**空欄を渡して書かせても、良い文章は出てこない。**
仕様書の方式（職人が説明文を書く → AIが3文に整える）は、
**元の文の水準が低いと、整った平凡な文章になるだけ**で終わる。

### 3-2. 解決の形

**AI の仕事は「文章の作成の補助」であって「校閲」ではない。**

```
1. 職人が5つの質問に答える（単語でもよい）
2. 答えが短い項目だけ、AIが1つ掘り下げる質問を返す
3. 集まった答えから、AIが日本語3文＋英訳を作る
4. 職人が直せる。直したものが保存される
```

### 3-3. 質問（5問）

**工芸品にも無形文化財にも使える形にする。**
「作る」「演じる」の両方に答えられる言い方を選ぶ。

| # | 質問 | 何を引き出すか |
|---|---|---|
| 1 | 何を作って（演じて）いますか。ひとことで。 | 主題 |
| 2 | その中で、いちばん手間がかかるのはどこですか。 | 具体的な工程 |
| 3 | そこは、ひとりでできるまでどれくらいかかりましたか。 | **年数。事実なので答えやすい** |
| 4 | よそと違うと思うのは、どんなところですか。 | 差 |
| 5 | はじめて見る人に、どこを見てほしいですか。 | 見どころ |

**3が肝。**職人は自慢が苦手だが、**年数は事実なので抵抗なく答えられる。**
そして「この工程だけで10年」は、それ自体が最も人を動かす情報になる。

伝統名・職人名・場所・タグは**フォームで既に取れている**ので質問しない。
入力の負担を15分以内に収めるため。

**この5問は仮ではなく、本書での決定とする。**変えるときは本書を先に直す。

### 3-4. AI の呼び方

```
文章生成   Gemini（下記）      日本語3文 + 英訳 を1回の呼び出しで
埋め込み   Workers AI bge-m3   検索の段3。既存のまま変更しない
ふりがな   Gemini（同上）      仕様書は gpt-oss-20b だが、モデルを増やしたくない
```

#### モデル名はエイリアスを使う

**`gemini-2.5-flash` はバージョン固定で指定すると 404 になる。**（2026-09-03 実測）

```
models/gemini-2.5-flash is no longer available to new users
```

ListModels には残っているが `generateContent` では拒否される。
**バージョンを固定すると、提供終了のたびに黙って止まる。**

そこで `-latest` エイリアスを使う。

```
gemini-flash-lite-latest   ← 採用
gemini-flash-latest        ← 使えなかった（下記）
```

**`gemini-flash-latest` は 503（高負荷）で使えない。**（2026-09-03 実測）
間隔を空けて6回試して6回とも失敗。`lite-latest` は安定して成功する。

**エイリアスは中身が勝手に変わるという欠点がある。**それでも固定より安全と判断した
（固定は今回 404 で止まった）。**発表の直前に1回、実際に叩いて確認すること。**

#### 【重要】503 は現実に起きる

`latest` が6回連続で 503 を返した。**`lite-latest` にも同じことが起きうる。**

そのため、フォールバックは「キーが無いとき」だけでなく
**「API が 5xx / タイムアウト / 不正な JSON を返したとき」にも必ず効くこと。**

```
どの失敗でも → ai:false でカードは作れる
              エラーを client に返して登録を止めない
```

**発表中に 503 が返ってカードが作れないのが、最悪の負け方。**
キー無しの経路だけ確認して安心しないこと。

（2026-09-03 決定。当初は「Gemini 2.5 Flash」と書いていたが、404 のため変更した）

**API の形**

```
POST /api/introduce/artisan/compose

リクエスト
{
  "name": "輪島塗",
  "answers": ["椀と箸", "下地を塗って研ぐ", "10年", "下地を20回重ねる", "縁の薄さ"],
  "followups": []                       // 省略可。既定は []
}

レスポンス（掘り下げが要るとき）
{ "ja": null, "en": null, "ai": true,
  "followup": { "index": 1, "question": "その工程で、いちばん失敗しやすいのはどこですか" } }

レスポンス（完成）
{ "ja": ["文1","文2","文3"], "en": ["s1","s2","s3"], "ai": true, "followup": null }
```

掘り下げが返ったら、フロントはその答えを `followups` に足して**もう一度同じAPIを呼ぶ。**

```json
"followups": [
  { "index": 1, "question": "その工程で…", "answer": "湿度が高い日は乾かない" }
]
```

**掘り下げは1周だけ。**`followups` が空でないリクエストには、
**必ず3文を返す**（`followup` を返さない）。

**なぜ1周に限るか：**無限に往復すると職人が離脱し、トークンも燃える。
要件は「初回15分以内」なので、往復は最小にする。

**サーバーは状態を持たない。**毎回 `answers` と `followups` を全部受け取って組み立てる。
セッションに途中経過を貯めない（実装が増え、消し忘れが出るため）。

**入出力の内訳**

```
入力  5問の質問と答え + 掘り下げの往復 + 伝統名 + 職人名 + 場所
出力  上記の JSON
```

**掘り下げの判定は2段に分ける。**（2026-09-02 決定）

| 何を | 誰が | なぜ |
|---|---|---|
| 空欄かどうか | **コード** | キーが無くても動作確認できる。元の質問をそのまま出し直すだけ |
| 薄いかどうか＋質問の文言 | **Gemini** | 具体的に聞けないと意味がない |

**空欄に「もう少し詳しく」と返すのは掘り下げではなく必須項目チェック。**
元の質問を出し直せば足りる。

**効くのは、答えはあるが薄いとき。**「いろいろ」と書かれたときに
「いろいろ、とは。漆を塗る回数ですか、乾かす時間ですか」と具体的に聞けると
良い材料が出る。これはコードでは書けない。

**文字数のしきい値は置かない。**「この答えでは3文が書けない」かどうかをモデルに判断させる。

**往復は増やさない。**同じ呼び出しの中で、3文を返すか `followup` を返すかを分岐させる。

#### 分野の観点をプロンプトに与える

伝統工芸・伝統芸能の説明文で説得力を生むのは何か、を Gemini に渡す。

```
工程の具体名        下地、研ぎ、燻し、地無し
回数・時間・年数    20回重ねる、10年かかる
材料の産地や状態
失敗しやすい条件    湿度が高い日は乾かない
```

これがあると「もう少し詳しく」ではなく「その下地は何回重ねますか」と聞ける。

#### 3文の書き方（プロンプトで指示する）

**事実だけ並べても平凡になる。**並べ方まで指示する。

| 守ること | なぜ |
|---|---|
| **1文目に、いちばん強い事実を置く**（年数・回数・具体的な数字） | 「10年」「20回以上」がその職人の凄み。埋もれさせない |
| **報告調にしない**（「〜である」「〜している」を避ける） | 第三者の解説文になり、作り手の顔が消える |
| **答えに無い形容詞を足さない**（丁寧な、美しい、伝統的な…） | 事実ではないうえ、どの工房にも書ける文になる |
| **質問と文を1対1で対応させない** | 穴埋めに見える。5問の答えを組み替えて3文にする |
| **短く言い切る** | 修飾を重ねるほど平凡になる |

悪い例（実際に出力されたもの）

```
椀と箸を手がける輪島塗の職人は、下地の漆を塗って研ぐ工程に最も手間をかけている。
その下地をひとりでできるようになるまでには10年かかり、20回以上も重ねるのが特徴である。
```

**何も嘘は書いていないが、一番強い「10年」「20回」が2文目に埋もれている。**
1文目は誰にでも書ける。「丁寧な手仕事」は答えに無い形容。

狙う形

```
下地の漆を、20回以上重ねる。
ひとりでできるようになるまでに10年かかった。
```

**この節は「答えに無いことを書かない」より優先度が低い。**
言い回しを整えるために事実を足すのは禁止。**足りないなら短い3文でよい。**

（2026-09-03 追加。実際の出力が平凡だったため）

#### 【最重要】分野の知識は質問にだけ使う

> **分野の知識は、質問を作るために使う。本文に事実を足すためには使わない。**
> **答えに書かれていないことは書かない。**足りないなら質問として返す。

ここを曖昧にすると、AI が「輪島塗は百以上の工程で分業され」といった**一般論を
勝手に本文へ書き込む。**それは職人の言葉ではないので、**この製品の価値そのものが消える。**
プロンプトに明示的に禁止として書くこと。

- **答えが空の項目があるときは、`followup` に元の質問をそのまま入れて返す（コード側）**
- **答えはあるが3文を書くには足りないときは、`followup` に具体的な質問を入れて返す（Gemini側）**
- 3文ちょうどにする。4文以上になったら**コード側で先頭3文を切り出す**（仕様書と同じ扱い）
- **答えに書かれていないことを足さない**ようプロンプトで明示する

**キーの扱い**

```
GEMINI_API_KEY   Workers の Secret。 npx wrangler secret put GEMINI_API_KEY
                 ブラウザには絶対に渡さない。フロントから直接叩かせない
```

**落ちたときの逃げ道（必須）**

Gemini が落ちても**カードは作れること。**
失敗したら、5問の答えをそのまま連結して `description` に入れて保存する。
レスポンスに `ai: false` を入れてフロントに伝える。
**発表中にAIが落ちてもデモが止まらないようにするため。**

---

## 4. 認証

### 4-1. パスワード

WebCrypto の PBKDF2-SHA256。ソルトは16バイト乱数。
**外部ライブラリを入れない**（Workers で動く保証を確かめる時間がない）。

**反復回数は 10,000。**`features/auth/crypto.js` の名前付き定数1箇所に置く。

```js
// 【重要】Workers 無料プランは CPU 10ms/リクエスト。
// 大きくすると本番の signup / login だけが落ちる。
// ローカルの wrangler dev では CPU 制限が効かないので再現しない。
const PBKDF2_ITERATIONS = 10000;
```

**これはデモ用の妥協であり、実運用の水準ではない。**

| | |
|---|---|
| OWASP の推奨 | PBKDF2-HMAC-SHA256 で **600,000回** |
| 本書の値 | **10,000回**（推奨の約60分の1） |
| なぜ下げたか | Cloudflare Workers 無料プランの CPU 上限が 1リクエスト 10ms。
10万回でも超える。**本番の signup / login だけが落ちる** |
| なぜローカルで気づけないか | `wrangler dev` は CPU 制限を強制しない。**デプロイして初めて落ちる** |
| 実運用でどうすべきか | Workers 有料プラン（CPU 30秒）に上げて反復回数を戻す。
またはハッシュ処理を別サービスへ逃がす |

**このアプリのアカウントはデモ用**で、実在の個人情報も決済情報も入れない。
発表中にログインできないほうが致命的、という判断で下げている。

**実データを入れる段階になったら、必ず見直すこと。**

（2026-09-02 決定。当初は10万回と書いていたが、CPU上限の調査により変更した）

### 4-1-b. パスワードの条件

**NIST SP 800-63B の現行版に合わせる。**

```
最小        12文字
最大        制限しない。切り詰めない
文字種の強制 しない（大文字・数字・記号を必須にしない）
使える文字   印字可能な文字すべて。スペースも Unicode も可
前後の空白   削らない（パスワードの一部として扱う）
拒否するもの よくあるパスワード上位100件 / メールアドレスと同一の文字列
```

**大文字・記号を必須にしない理由：**必須にすると人は `Password1!` を作る。
規則は満たすが辞書攻撃に弱い。しかも規則を満たすために短くなる。
NIST は現行版で **composition rules を明示的に廃止**している。効くのは長さ。

**なぜ NIST の下限8ではなく12にしたか：**
このプロジェクトは PBKDF2 が1万回（推奨の60分の1）と弱い。
ハッシュが弱いぶん、流出時の総当たりが安くなるので、長さで補う。

**なぜ NIST 推奨の15にしなかったか：**
発表で壇上から打つとミスするため。**デモ用アカウントは事前に作っておくこと。**

**流出パスワードの照合（Have I Been Pwned API）は入れない。**
signup ごとに外部通信が増え、落ちたときの分岐も増える。
埋め込みの上位100件で、デモで踏む範囲は防げる。

（2026-09-02 決定）

### 4-2. セッション

乱数32バイトの hex を `sessions` に保存し、Cookie で返す。

```
Set-Cookie: sid=<token>; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=604800
```

**JWT を使わない理由：**失効させられない。デモ中に消したくなったときに困る。
DBに置けば `delete from sessions` で全部切れる。

**`HttpOnly` にする理由：**JS から読めなくする。XSS でトークンを盗まれないため。

**`SameSite=Lax` でよい理由：**フロントエンドを**この Worker と同じところに載せる**ため
（2026-09-02 確認済み）。同一オリジンなので Cookie は問題なく送られる。

**別オリジンにするなら `SameSite=None; Secure` が要る。**そのときは常時HTTPSが前提になり、
ローカル確認の手順も変わる。**構成を変えるなら、実装より先にここを直すこと。**

### 4-3. ミドルウェア

```js
// 【重要】"*" で登録しない。他の機能に漏れる（AGENTS.md 2-c）
app.use("/api/introduce/artisan/*",     requireAuth);   // 職人側は全部
app.use("/api/introduce/user/liked",    requireAuth);   // いいね一覧
app.use("/api/introduce/user/:id/like", requireAuth);   // いいねトグル
app.use("/api/auth/me",                 requireAuth);
```

**閲覧は認証を掛けない。**次の2つは未ログインでも通す。

```
GET /api/introduce/user/next    カードを1枚
GET /api/introduce/user/:id     詳細
```

**理由：**このアプリは訪日客が偶然出会うことを前提にしている。
質疑応答でも「DLが必要なアプリではなく、Webで作る」と答えている。
**入口でログインを要求したら成立しない。**

フロントエンドの画面優先順（8/28確定）でも
「ホーム → カード閲覧 → 職人プロフィール → 検索結果 → **ログイン**」の順で、
**カード閲覧がログインより前**に来る。

`/api/introduce/user/*` にまとめて認証を掛けると、
**未ログインではカードが1枚も見られない**状態になる。
しかも壊れ方が地味で、通し確認まで気づけない。

（2026-09-02 修正。当初は `/api/introduce/user/*` 全体に掛けると書いていた）

未ログインは 401 `{ error: { code: "UNAUTHORIZED" } }`。

**職人側のAPIは `role === 'artisan'` を確認する。**
`artisan_id` はクエリではなく**セッションから取る**（仕様書の簡易実装はなりすませる）。

---

## 5. 英語対応

カード作成時に日本語と英語を同時に生成し、`card_i18n` に入れる。
**検索時・表示時に翻訳しない**（コストが閲覧回数に比例するため）。

```
GET /api/introduce/user/:id?lang=en   → card_i18n があれば差し替えて返す。無ければ日本語
```

---

## 6. 画像

**Cloudflare Workers KV に、生のバイト列のまま保存する。**
R2 は課金設定が要るため不採用。Supabase Storage は筋がいいが、
2日で2つ目のデータストアを足す余裕がないため見送る。

**base64 にしない。**（仕様書は base64 だが、こちらは変える）

```
5MB の画像 → base64 で約6.7MB
取り出すたびにデコードが必要 → Workers 無料プランは CPU 10ms/リクエスト
```

**PBKDF2 と同じ「ローカルでは通って本番だけ落ちる」型になる。**
KV は `ArrayBuffer` をそのまま `put` できるので、base64 にする必要がない。
デコードが消えて CPU を食わず、容量も33%減る。

取り出しは `get(key, "stream")` して、そのままレスポンスに流す。

```js
await c.env.teame_images.put(key, arrayBuffer, {
  metadata: { content_type: file.type },   // 何の画像かを覚えておく
});

const { value, metadata } = await c.env.teame_images.getWithMetadata(key, "stream");
return new Response(value, { headers: { "content-type": metadata.content_type } });
```

**`metadata` に `content_type` を必ず入れること。**
入れないと、取り出したときに何の画像か分からず返せない。

（2026-09-02 決定。当初は仕様書に合わせて base64 と書いていたが、CPU上限のため変更した）

```
バインディング   teame_images   KV Namespace
キー             card-image:{uuid}
上限             1枚5MB / 1カード3枚まで（サーバー側でも強制する）
許可             image/* のみ
```

```
POST /api/introduce/artisan/upload-image   1枚ずつ。 → { key }
GET  /api/introduce/image/:key             KVから取り出してデコードして返す
```

登録時に `image_keys` の配列を送り、`card_images` に `sort_order` 付きで保存する。

**`GET /image/:key` は認証を掛けない**（カード閲覧で誰でも見るため）。
ただし**キーは uuid なので推測できない。**

---

## 7. 住所と地図

**Google Maps を使わない**（チーム方針）。すべて OpenStreetMap 系で揃える。

| 用途 | 使うもの | キー |
|---|---|---|
| 郵便番号 → 住所 | zipcloud API | 不要 |
| 屋号・住所 → 候補＋緯度経度 | Nominatim（OSM） | 不要 |
| 緯度経度 → 住所（地図クリック） | Nominatim 逆引き | 不要 |
| 地図表示 | Leaflet + OSM タイル | 不要 |

```
GET /api/introduce/artisan/postal?code=9280001     zipcloud を中継
GET /api/introduce/artisan/geocode?q=輪島塗会館     Nominatim を中継（上位3件）
GET /api/introduce/artisan/reverse?lat=&lng=       Nominatim 逆引き
```

**サーバー側で中継する理由：**ブラウザから直接叩くと CORS と
Nominatim の利用規約（User-Agent 必須）を守れないため。

**Nominatim の制約：** 1秒1リクエストまで。`User-Agent` を必ず付ける。
個人の小さな工房はヒットしないことが多い（仕様書も指摘済み）。
**そのときは「郵便番号で大まかに → 地図をクリックして正確な位置」**の経路を使う。

緯度経度は `card_geo` に保存する。無い場合は検索機能と同じく
都道府県の代表座標（`features/search/regions.js` の `coordsOf`）で代替する。

地図表示は `features/search/ui.js` の実装を持ってくる。
**CDN が落ちる場合に備えて try/catch で包む**（AGENTS.md の落とし穴）。

---

## 7. ディレクトリ

```
features/introduce/
├ index.js        ルーティング
├ db.js           D1 アクセス
├ compose.js      5問 → Gemini → 3文＋英訳
├ prompt.js       プロンプトと質問の定義
└ ui.js           /dev/introduce（動作確認用。成果物ではない）

features/auth/
├ index.js        signup / login / logout / me
├ crypto.js       PBKDF2
└ session.js      Cookie とセッション
```

登録は `features/auth/` に含める（要件定義書 3-4 のとおり最小限にするため、
分ける意味がない）。`features/register/` は削除した（8-3参照）。

---

## 8. 検索機能との接続（忘れやすい）

**カードを作っただけでは、段3（あいまい検索）に出てこない。**
`card_embedding` に埋め込みが無いため。

```
POST /api/introduce/artisan   → カード保存後、そのカードの埋め込みを1件だけ生成する
```

全件 reindex を呼ばない。**1件だけ**生成する。
失敗しても**カードの保存は成功させる**（段1・段2では検索できるため）。

**`card_embedding` に直接 INSERT を書かないこと。**
このテーブルは検索機能の所有物で、`features/search/db.js` に
「検索側が書き込む唯一の場所」と明記してある。
紹介機能から生SQLを書くと、所有権の原則（AGENTS.md 絶対に守ること1）が形骸化する。

**検索機能が公開している関数を import して呼ぶ。**

```js
import { embed, cardToText, EMBED_MODEL } from "../search/embed.js";
import { saveEmbedding } from "../search/db.js";

// カード保存後
try {
  const [vec] = await embed(c.env.AI, [cardToText(card)]);
  await saveEmbedding(db(c), card.id, vec, EMBED_MODEL);
} catch (e) {
  console.error("[introduce] 埋め込み生成に失敗", e);  // 保存は成功させる
}
```

**これは所有権の違反ではない。**テーブルを直接触るのが違反であって、
所有者が公開している関数を呼ぶのは正しい使い方。

---

## 8-2-b. 英語クエリを当てる（2026-09-03 決定）

**検証の結果、英語クエリは機能しないことが確定した。**

```
候補25件すべてに埋め込みを付けた状態で
  "lacquer bowl"  → 0件（輪島塗が候補にいた上で）
  "bamboo flute"  → 0件（尺八が候補にいた上で）
```

要件定義書は「訪日客が主、日本語話者が従」としているので、これは要件に対する穴。

### 直し方

**埋め込みを作るとき、日本語の説明文に英訳も足して1つのベクトルにする。**

```
いまの入力   cards の日本語の列だけ
これから     日本語の列 + card_i18n(lang='en') の name / description
```

bge-m3 は多言語モデルなので、1つのベクトルに両方の言語を入れられる。
英語クエリは英訳の部分に直接当たる。

**`features/search/` を変更する。**この機能は「完成済み・触るな」としていたが、
要件に対する穴なので例外とする。**変更するのは埋め込みの入力テキストだけ。**
段1・段2、フィルタ、UI には触らない。

**`card_i18n` は紹介機能の所有テーブルだが、検索側は読むだけ。**
`cards` を読んでいるのと同じ扱い。書き込まない。

#### 効果（2026-09-03 実測）

```
bamboo flute        → 尺八 0.529          英訳を入れる前は0件
lacquer bowl        → 輪島塗 が1位に浮上
traditional textile → 本場大島紬 0.474    英訳が無いカードにも当たっている
```

多言語モデルなので、**英訳を入れていないカードにも部分的に当たる。**

**変更したら全件を作り直すこと**（`POST /api/search/reindex`）。
古い埋め込みは日本語だけで作られているので、混在すると結果が歪む。
`card_embedding` を空にしてから reindex する。

---

## 8-2-c. 段3のしきい値（測ってから決める）

いまは絶対値 `0.45`（`features/search/search.js` の `VECTOR_THRESHOLD`）。

**実測すると、上位が 0.450〜0.466 に固まっていた。**

```
「静かな雰囲気のもの」
  0.466  京友禅 手描きの風呂敷
  0.457  大館曲げわっぱ 弁当箱
  0.450  播州そろばん 樺の玉
```

**0.016 の幅。**「この3件が近い」のではなく「全部が同じくらい遠い」状態で、
順位はほぼ誤差。しきい値を少し動かすだけで結果が入れ替わる。

### 決め方

**数値を想像で置かない（作業ルール8）。まず測る。**

1. 8-2-b（英語対応）を適用して全件を作り直す
2. 日本語・英語それぞれ数本のクエリで、**しきい値に関係なく上位5件のスコアを記録する**
3. その分布を見てから、絶対値をやめて上位N件で切るか、floor をいくつにするかを決める

**測る前に実装しない。**

### 測った結果と決定（2026-09-03）

全22件に英訳込みの埋め込みを付けた状態で、10クエリの上位5件を記録した。

**絶対値 0.45 は、意味的に正しい答えを捨てていた。**

```
lacquer bowl
  0.425  輪島塗      ← 1位。正解。なのに 0.45 未満で0件だった
  0.386  京友禅 手描きの風呂敷
```

**【訂正】**当初ここに「子どもと一緒に体験できるもの」も0件の例として挙げていたが、
**誤りだった。**あの測定は段を通さない生のスコアで、実際のAPIでは
**段2（「体験」がシノニムでヒット）で確定して2件返っていた。**
製品としては最初から正しく動いていた。（2026-09-03 訂正）

段3まで落ちるクエリで、絶対値0.45が正解を捨てていたのは `lacquer bowl` 等。

**分布に2つの型があり、1本の絶対値では扱えない。**

```
bamboo flute       0.529 → 0.416          1位だけ突出（差 0.113）
子どもと一緒に…    0.429 → 0.424 → 0.411  なだらか
```

#### 決定

```
最高スコアから 0.06 以内のものを残す。最大5件。ただし 0.35 未満は捨てる
```

- **突出した1件があるときは絞る**（bamboo flute → 尺八だけ）
- **なだらかなときは広げる**（子どもと一緒に… → 5件）
- **0.35 の下限は、何も近くないときに無関係なものを出さないため**

`VECTOR_THRESHOLD`（絶対値）は廃止する。
代わりに `VECTOR_REL_WINDOW = 0.06` / `VECTOR_FLOOR = 0.35` / `VECTOR_LIMIT = 5`。

**この数値は上記の実測から決めた。想像で置いていない。**
実データが増えたら測り直すこと。

#### 無関係なクエリは、しきい値では弾けない（2026-09-03 実測）

「最高スコアが低すぎるクエリは丸ごと捨てる」というゲートを検討し、
無関係なクエリ10本を測った。**結論：2群は重なっていて線を引けない。**

```
本物のクエリ    0.425 〜 0.54   （最低は lacquer bowl）
無関係クエリ    0.283 〜 0.432  （最高は「ラーメンが食べたい」）
```

「ラーメンが食べたい」(0.432) が「lacquer bowl」(0.425) より高い。
**どこに線を引いても、本物を落とすか偽物を通すかのどちらかになる。**

**ゲートは足さない。**

#### なぜ弾けなかったか

上位に来たものを見ると、**モデルは意味のある関連を拾っていた。**

```
ラーメンが食べたい  → 大館曲げわっぱ 弁当箱   食べ物を入れる器
my cat is sick     → 三味線 紅木の棹        三味線の皮は猫皮
```

**「無関係」と決めつけたのはこちら側で、モデルは間違っていない。**
機械的にノイズを弾こうとしたのが筋悪だった。

#### 代わりにどうするか

**弾かずに、見せ方で扱う。**

段3の結果は「一致」ではなく**「近いもの」**として見せる。
APIは `stage` を返しているので、フロントが `vector` のときだけ
見出しを変えればよい（「もしかして、こういうものですか」など）。

**これは逃げではない。**あいまい検索は本来そういう機能で、
**確実な一致のふりをするほうが嘘になる。**

発表では、当たりの良いクエリを選んで見せればよい。
それは「都合のよい例を選んだ」のではなく、**機能の説明として適切な例を選んだ**ということ。

---

## 8-2. 英語クエリと段3（検証済み。8-2-b で対応）

段3の埋め込みは `cards` の日本語列（`cardToText`）から作っている。
`card_i18n` の英語は埋め込みの対象外。

**「だから英語では検索できない」とは限らない。**
使っているモデル `@cf/baai/bge-m3` は**多言語対応で、
言語をまたいだ検索（cross-lingual retrieval）を想定して作られている。**
英語のクエリが日本語の説明文に当たる可能性は十分ある。

**未検証。**英語で1回投げれば確定する。ステップ8で確認する。

当たらなかった場合の選択肢（**今は決めない**）

- 英語の説明文からも埋め込みを作り、`card_embedding` に lang 付きで持つ
- 英語クエリは段1・段2（キーワード・シノニム）だけで受ける

---

## 8-3. `features/register/` を削除した（2026-09-03 決定を変更）

登録機能は `features/auth/` にまとめている（要件定義書 3-4 のとおり最小限にするため）。

当初は「登録機能の担当者が戻ってくる可能性がゼロではないので、
`features/register/` のスタブを残しておく」という方針だった。
**2026-09-03、担当者はもう戻ってこない想定になったため、この前提が崩れた。**

スタブを残す実害も見つかった。`features/register/index.js` の
`app.get("/register", notImplemented)` が、フロントエンドの18画面にある
サインアップ用パス `/register` を**本番で塞いでいた。**Hono は先に登録された
ほうが勝ち、エラーも警告も出ないため、フロント側からは原因が分からない状態だった。

塞いでいる意味が無くなったため、`features/register/` はディレクトリごと削除した。
登録機能自体は `features/auth/` に統合済みなので、機能は失われない。

---

## 9. 未決事項

| 項目 | 状況 |
|---|---|
| メール認証 | **余力があれば。**無料で送れる手段を確認していない |
| タグの選択UI | `cards.tags` に入れる形は決まっている。**選ばせ方は未決** |
| `/dev/introduce` の見た目 | フロントエンドの担当。こちらは確認用のみ |
| KV の無料枠 | 書き込み 1,000/日。デモなら足りるが**未確認** |
