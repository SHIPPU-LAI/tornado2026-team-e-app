# ログイン機能 ── 未着手

いまは 501 を返すスタブです。**枠があることを示すためだけのもの**で、中身はありません。

## 分かっていないこと

**このドキュメントは意図的にほとんど空です。想像で埋めていません。**

| 項目 | 状況 |
|---|---|
| **担当者** | **未定**。誰がやるか決まっていない |
| 着手しているか | 不明。誰かが作っているかも把握できていない |
| 認証方式 | 未定 |
| 持つパス | 未定（`/login` と `/api/auth/*` は仮置き） |
| 所有するテーブル | 未定 |
| 職人とユーザーで分けるか | 未定 |

## 分かっていること

- **作る予定がある**、ということだけ

## 関連する未決事項

- **ユーザー登録機能（`features/register/`）との境界が不明。**
  登録とログインを同じ担当が持つのか、分けるのかが決まっていない
- 紹介機能の仕様書には
  「認証機構（現状は artisan_id / user_id をクエリ・body で直接受け渡す簡易実装、
  USER_ID = 'test' 固定）」と**未実装である旨の記載**がある。
  ログイン機能が入ると、ここを差し替える必要がある

## 持ち込み方

1. `features/auth/index.js` を実装で置き換える
2. `src/index.js` の import はそのままでよい（パスが同じなら）
3. マイグレーションが要るなら `migrations/` に新規追加する（既存ファイルは書き換えない）
