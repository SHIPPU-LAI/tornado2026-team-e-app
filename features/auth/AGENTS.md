# ログイン機能 ── AIへの指示

**まずリポジトリ直下の [AGENTS.md](../../AGENTS.md) を読んでください。**

## あなたが触っていい場所

```
features/auth/     ← ここだけ
```

## いまの状態

**未着手のスタブです。担当者すら決まっていません。**

## ほぼ全てが未定です

**この機能は、想像で実装を始めてはいけません。**

| 項目 | 状況 |
|---|---|
| 担当者 | **未定** |
| 認証方式 | 未定（セッション / JWT / Cloudflare Access など何も決まっていない） |
| 持つパス | 未定（`/login`、`/api/auth/*` は仮置き） |
| 所有するテーブル | 未定 |
| 職人とユーザーを分けるか | 未定 |
| 登録機能（`features/register/`）との境界 | 未定 |

分かっているのは「**作る予定がある**」ことだけです。

**実装を依頼されたら、まず上の項目を人間に確認してください。**
決めずに書き始めると、ほぼ確実に作り直しになります。

## 既存の実装への影響

紹介機能の仕様書に、こう書かれています。

```
認証機構（現状は artisan_id / user_id をクエリ・body で直接受け渡す簡易実装、
USER_ID = 'test' 固定）
```

つまり**いまは認証が無い状態で他の機能が動いています。**
ログイン機能を入れると、他の機能のAPIも変更が必要になります。

**その変更は他の担当のディレクトリで行うものなので、勝手に書き換えないでください。**
「ここを変える必要がある」と報告するに留めてください。
