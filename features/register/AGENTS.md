# ユーザー登録機能 ── AIへの指示

**まずリポジトリ直下の [AGENTS.md](../../AGENTS.md) を読んでください。**

## あなたが触っていい場所

```
features/register/     ← ここだけ
```

## いまの状態

**スタブです。**501 を返すだけで、中身はありません。

## 決まっていないことが多い機能です

**想像で埋めないでください。**以下は全て未確定です。

| 項目 | 状況 |
|---|---|
| 持つパス | 未定（`/register`、`/api/register/*` は仮置き） |
| 所有するテーブル | 未定 |
| 認証の扱い | 未定 |
| ログイン機能（`features/auth/`）との境界 | **未定** |

分かっているのは、MTG議事録の以下だけです。

```
ユーザー登録
  終わりの目安：新規登録、編集が行える
```

実装を始める前に、**まず人間に「どこまでがこの機能の範囲か」を確認してください。**

## 注意：他の機能と重複している可能性があります

アイデアソン時点で挙がっていた項目のうち、職人側のもの
（屋号・住所・伝統の歴）は、**紹介機能のカード作成と重複しています。**

どちらが持つかが決まっていません。**決まるまで、この項目には手を付けないでください。**
「片方だけなら実装してよい」という意味ではありません。

## 新しいテーブルを作るとき

```bash
npx wrangler d1 migrations create teame register_init
```

既存のマイグレーションは書き換えないこと。他の機能のテーブルにも触らないこと。
