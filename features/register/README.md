# ユーザー登録機能 ── 持ち込み待ち

**作業ディレクトリ：`features/register/`**

いまは 501 を返すスタブです。**枠があることを示すためだけのもの**で、中身はありません。

## 分かっていないこと

**このドキュメントは意図的にほとんど空です。想像で埋めていません。**

| 項目 | 状況 |
|---|---|
| 担当者 | 記載を避ける（役割で管理） |
| 進捗 | 不明 |
| 実装場所（リポジトリ） | 不明 |
| 持っているパス | 不明 |
| 所有するテーブル | 不明 |
| 認証の扱い | 不明 |

分かり次第ここに追記してください。

## 決まっていることだけ

8/20 のMTG議事録より:

```
ユーザー登録
  終わりの目安：新規登録、編集が行える
```

必須機能のリストに挙がっていた項目（アイデアソン時点。確定ではない）:

```
国籍 / 年齢 / ユーザー名 / 言語能力 / 好きな伝統文化（お気に入りのタグ登録） /
日本語能力 / 日本在住の有無 / 伝統の歴（何年創業か、何代目か） /
店名（屋号）・住所 / 地域
```

※ このうち職人側の項目（屋号・住所・伝統の歴）は、
**紹介機能のカード作成と重複している可能性があります**。役割分担は未確認。

※ **ログイン機能（`features/auth/`）との境界も未定です。**
登録とログインを同じ担当が持つのか分けるのかが決まっていません。

## 持ち込み方

1. `features/register/index.js` を実装で置き換える
2. `src/index.js` の import はそのままでよい（パスが同じなら）
3. マイグレーションを `migrations/` に追加する（既存ファイルは書き換えない）
