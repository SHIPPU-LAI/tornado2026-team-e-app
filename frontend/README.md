# フロントエンド

**画面（見た目）はここが担当します。**

バックエンドの `features/*` は API を提供するだけで、
`/dev/*` にある画面はバックエンドの動作確認用です。成果物ではありません。

## パスは空けてあります

バックエンド側は本番用のパスを占有していません。ここに置いてください。

| パス | 用途 | 現状 |
|---|---|---|
| `/` | ホーム画面 | いまは統合状況の一覧が出ている（差し替え可） |
| `/search` | 検索結果画面 | **空き** |
| `/user` | マッチング画面 | 紹介機能のスタブが 501 を返している |
| `/login` | ログイン画面 | ログイン機能のスタブが 501 を返している |

`/dev/search` はバックエンドの確認用なので、そのまま残しても消しても構いません。

## 進捗（共有ドキュメントより）

**この節は共有ドキュメントの記載を写したものです。実装は確認していません。**

### 終わっているもの

```
デザイン    ホーム画面（非ログイン）／のれん→ズームイン／
            カード（右=保存・左=破棄）／お気に入り
フロント    ホーム画面／のれんアニメーション／シルエット表示／
            カード画面／カードのスワイプ機能／ハンバーガーメニュー
```

### 未完成

```
デザイン    プロフィール画面（名前→工房）／検索結果画面／投稿画面／オプション（職人）
フロント    のれんの動作・デザイン修正／ジャンル選択→カード画面の遷移／
            アプリ目的の詳細表示／保存・削除のイラスト設定／タグの動作設定／
            お気に入り画面（バックとつなぐ・タグ別表示）
```

### 決まっているもの

```
フォント    Zen Kaku Gothic Antique（普通）
            Zen Antique Soft（おしゃれ）
参考        https://bg-patterns.com/?cat=4&paged=11
```

### 必要な素材（未用意）

```
のれん png ／ 雲 ／ 和柄（大きめの写真）／ 色（HEXコード）／
カードの保存・破棄ボタン ／ 各種アイコン（お気に入り・破棄・検索）
```

素材は `public/assets/` に置いてください。

## 未決事項

| 項目 | 状況 |
|---|---|
| **フロントの構成** | **未定**。SSR（Hono JSX）か、静的HTML＋JSか、SPAか決まっていない |
| このディレクトリを使うか | 未確認。枠を用意しただけ |
| スワイプの「次に何を出すか」 | フロントでスワイプUIは実装済みだが、**出す順・見たものの記録をどのAPIが担うか未定** |
| ホーム画面 `/` の差し替え | いまはバックエンドの統合状況一覧。いつ誰が差し替えるか未定 |

## 地図について

**Google Maps API は使わない方針です。**APIキーも課金設定も要らない構成にしています。

```
バックエンドの検証用画面    Leaflet + OpenStreetMap
外部地図へのリンク          OpenStreetMap
```

地図を画面に載せる場合も、この方針に合わせてください。
（紹介機能の仕様書には「Google Maps embed（APIキー不要）」の記載があるので、
そこと整合を取る必要があります）

## バックエンドのAPIを叩くとき

エラーは全機能共通でこの形です。

```json
{ "error": { "code": "NOT_IMPLEMENTED", "message": "..." } }
```

`501` が返ってきたら、その機能はまだ統合されていません。
使えるAPIは各 `features/*/README.md` を見てください。
